const COLORS = {
  indi: {
    400: '#877dfa',
  },
};

const FONT_SIZE = {};

const FONT_FAMILY = {};

const GRADIENTS = {};

const SHADOWS = {};

module.exports = {
  colors: COLORS,
  fontSize: FONT_SIZE,
  fontFamily: FONT_FAMILY,
  gradients: GRADIENTS,
  shadows: SHADOWS,
};
